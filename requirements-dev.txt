# Dev/build dependencies — ไม่ได้ใช้ใน production image
# ใช้สำหรับ convert HuggingFace -> ONNX, รันเทส, benchmark

-r requirements.txt

# Model conversion
torch==2.5.1
transformers==4.47.1
huggingface-hub==0.27.0

# Testing
pytest==8.3.4
httpx==0.28.1   # สำหรับ TestClient

# Linting (optional)
ruff==0.8.4
